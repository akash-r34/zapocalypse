# /remember — Save to Project Memory

Save the following to the appropriate memory file in `.claude/memory/`:

$ARGUMENTS

Rules:
- If it's a decision → append to `decisions.md` with today's date and **Why** + **How to apply** lines
- If it's a phase update → update `phase_status.md` checkbox
- If it's a user preference or correction → update `user_prefs.md`
- If it's a new constraint → update `project_context.md`
- Always update the "Quick Status" block in `MEMORY.md` if the current phase changed
